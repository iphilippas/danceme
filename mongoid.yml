test:
  sessions:
    default:
      database: danceme_test
      hosts:
        - localhost:27017
development:
  sessions:
    default:
      database: danceme_dev
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ<em>URL'] %>
      options:
        skip_version_check: true
        safe: true
